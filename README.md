# Extracting-tech-skills-from-job-description
A lightweight NLP system that automatically extracts IT skills from job descriptions using a fine-tuned DistilBERT token classifier. The pipeline handles synonym normalization, span detection, BIO tagging, and sub-token alignment — trained and evaluated entirely on real recruitment data.
